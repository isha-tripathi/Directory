<h1>All Entries</h1>
<% if @entries.empty? %>
    <h3>There are no entries for this batch. Add a new one? </h3>
<% else %>
    <table>
    <thead>
        <td class="table-elem"><b>Name</b> </td>
        <td class="table-elem"><b>Phone Number</b></td>
    </thead>

    <% @entries.each do |entry| %>
        <tr>
          <td class="table-elem"><%= entry.name %></td>
          <td class="table-elem"><%= entry.phone_number %></td>
        </tr>

    <% end %>
    </table>
<% end %>

<%= button_to 'New Entry', new_entry_path, method: 'get' %>